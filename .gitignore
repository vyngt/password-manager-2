draft/
.vscode/
target/
.claude/

*.env


# Dev-profile app data: vedge-tauri points `app_dir` here under
# `cfg(debug_assertions)` so wiping state is `rm -rf local/`.
local/

# E2E driver
msedgedriver.exe

# Scale-test CSVs produced by scripts/gen_import_csv.ps1 — generated artifacts, not source
# (large + full of throwaway plaintext credentials).
vedge-import-*.csv
