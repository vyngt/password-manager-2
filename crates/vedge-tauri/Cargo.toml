[package]
name        = "vedge-tauri"
version.workspace = true
description = "Vedge Tauri shell"
authors     = ["VyNT <vyngt@outlook.com>"]
edition     = "2024"
publish     = false
default-run = "vedge-tauri"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix keeps the lib name distinct from the bin on Windows.
# See https://github.com/rust-lang/cargo/issues/8519.
name       = "vedge_tauri_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
vedge-core = { path = "../vedge-core" }
vedge-ipc  = { path = "../vedge-ipc" }

tauri                             = { version = "2", features = [] }
tauri-plugin-dialog               = "2"
# Kept for its `reqwest` re-export (HIBP, `breach/hibp.rs`) — NOT registered as an
# IPC plugin (see lib.rs), so the frontend has no arbitrary-HTTP capability. The
# other migrated plugins (shell/notification/process/fs/clipboard-manager/os/
# global-shortcut) were unused by both frontend and Rust and were dropped (PG.2b).
tauri-plugin-http                 = "2"

serde       = { version = "1", features = ["derive"] }
serde_json  = "1"
tokio       = { version = "1", features = ["sync", "rt-multi-thread", "macros", "time"] }
tracing     = "0.1"
thiserror   = "2"
async-trait = "0.1"
base64      = "0.22"
chrono      = { version = "0.4", default-features = false, features = ["std", "serde"] }
secrecy     = "0.10"
zeroize     = "1"

# Emergency Kit PDF rendering. Both crates are pure-Rust and pull in no C deps.
printpdf    = { version = "0.7", features = ["embedded_images"] }
qrcodegen   = "1.8"

[target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
tauri-plugin-single-instance = "2"

[dev-dependencies]
tempfile = "3"

[features]
# DO NOT REMOVE — used by `cargo tauri build` / `cargo tauri dev`.
custom-protocol = ["tauri/custom-protocol"]

# Feature flags mirrored from vedge-core — bodies land when the modules do.
pki            = []  # later: ["vedge-core/pki"]
steganography  = []  # later: ["vedge-core/steganography"]
sync           = []  # later: ["vedge-core/sync"]
full           = ["pki", "steganography", "sync"]

[lints]
workspace = true
