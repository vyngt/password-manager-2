[package]
name = "vedge-ipc"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["alloc", "serde"] }
thiserror = "2"
base64 = "0.22"

[lints]
workspace = true
