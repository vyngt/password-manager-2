# Slice 2.9.1 — Layout, Forms & Scrollbars

The layout/form-ergonomics/scrollbar half of polish (sibling to 2.9's feedback/a11y). Fixes the rough
edges a 2026-07-07 audit found: entry **edit** was squeezed into the 320px detail rail, the SSH private
key was an unusable single-line masked input, there was no minimum window size (the three-pane
overflowed), long table cells forced a horizontal scrollbar, and scrollbars were the native OS ones.
Spec: `.../Specs/2.9.1 - Layout Forms and Scrollbars`. Mockup: `2.9.1 - Roomy Edit Form`.

## Spec-vs-reality corrections (recorded as DRIFT in the tracking note)

- **`DialogSize::Lg` already existed** (`tokens.rs`, CSS `max-width:720px` in `dialog.css`) and
  `.dialog__body` already scrolls internally — the spec's "add `DialogSize::Lg`" work was already done.
  We just pass `size=DialogSize::Lg`.
- **`Textarea` has no reveal/mask** (a `<textarea>` can't use `type="password"`) and its callback is
  `on_change`, not `on_input`. The PEM reveal is net-new (a `masked` prop + `-webkit-text-security`).
- **`Input`'s root is a wrapper `<div>`** with the native `<input>` as a child bound to a *private*
  ref + `on:input:target`. So `node_ref`/`autofocus` had to be **explicit props** bound to the
  `<input>` (spreading them onto `<Input>` would hit the wrapper `<div>`).
- **The spec's create/edit ambiguity** (item #1/module-spec say both open the dialog; item #6 caps the
  create panel width → stays inline) was resolved in favour of **#6**: create stays an inline
  (width-capped) panel, only **edit** moves into the dialog; both consume one extracted `EntryFormBody`.

## What shipped

### vedge-ui

- **`Input` gains `input_ref` + `autofocus`** (`form/input.rs`): an optional external
  `NodeRef<html::Input>` (reconciled with the private clear-focus ref — search-clear still refocuses the
  same element) and an `autofocus` flag, both bound to the native `<input>`. Default-omitted = today's
  behaviour, so the existing call sites are untouched. (Prerequisite for the deferred inline-field
  conversions — see Deferred.)
- **`Textarea` gains a reactive `masked` prop** (`form/textarea.rs` + `.textarea--masked` in
  `textarea.css`): toggles `-webkit-text-security: disc` on the inner `<textarea>` (honoured by both
  target webviews). The masked class is a `class=(…, move || …)` directive on the element, so it doesn't
  fight the component's imperative auto-resize (which owns the `style` attribute, not `class`).
- **Custom scrollbars** (`styles/scrollbar.css`, imported in `index.css`): a thin, token-driven
  `::-webkit-scrollbar` (thumb = `--color-border-strong`, hover = `--color-text-tertiary`, transparent
  track, inset via a padding-box border) that follows whatever theme Rust injects at `:root`. Scoped to
  `::-webkit-scrollbar` only — deliberately **no** `scrollbar-width`/`scrollbar-color`, which in recent
  Chromium disable the pseudo-elements and would drop the styling.

### vedge-app — roomy shared create/edit surface

- **New `EntryFormBody`** (`features/vault/entry_form_body.rs`): the shared form surface = an optional
  type picker (create only — an entry's type is immutable after create) + `<EntryForm>` + `<FolderSelect>`
  + a Cancel/Save footer. It owns no persistence; the host passes `on_save`/`on_cancel` (`Callback<()>`)
  and a `saving` flag (the footer disables + spins Save while saving or the name is blank).
- **`VaultCreateForm`** renders `<EntryFormBody show_type_picker=true>` inline and is now width-capped
  (`max-w-2xl`) so the 2-col form doesn't stretch full-monitor-wide.
- **`VaultDetail`'s Edit** opens `<EntryFormBody show_type_picker=false exclude_id=…>` inside a large
  `Dialog` (`DialogSize::Lg`) instead of mounting the same form in the 320px read rail. Save/Cancel are
  now `Callback<()>`; the dialog's scrim/Escape close routes to cancel. While editing, the compact read
  aside collapses to just its header (the modal is the focus). The command-palette edit path
  (`ui.edit_request` Effect) is unchanged. The reveal-to-edit path (`get_entry`) is untouched — no new
  plaintext crosses to WASM.

### vedge-app — SSH PEM textarea

- **`entry_form.rs`**: the SSH `private_key` field is now a `col-span-2` monospace `<Textarea rows=4
  max_rows=12>` with a reveal `IconButton` header, replacing the single-line masked `secret_field!`.
  The model field stays a `String` and its payload mapping is unchanged, so `to_payload`/`from_payload`
  tests stay green. The reveal is display-only (`masked` toggle) over the already-revealed model value.

### vedge-app — layout guards

- **`tauri.conf.json`**: `minWidth: 960`, `minHeight: 600` — the three-pane no longer overflows.
- **`vault_table.rs`**: Name and URL cells truncate (`max-w-[…] truncate` + `title` tooltip, matching
  the folder/tag pattern); the scroll container gets `min-w-0` so one long URL can't force a horizontal
  scrollbar and shove the Actions column off-screen.
- **`vault_detail.rs`**: the read aside yields on narrow (`w-80 min-w-0`, dropping the hard `shrink-0`).
- **`vault_setup.rs`**: the onboarding container gets `overflow-y-auto` so the Emergency-Kit step can't
  clip at 600px height.

### vedge-app — accessibility

- **The 4 `window_panel` chrome icons** 2.9 left un-hidden (min/max/restore/close) are now wrapped in
  `<span aria-hidden="true">`, completing the 2.9 icon sweep. The wrapping-element pattern sidesteps the
  Leptos RPIT capture bound that `attr:aria-hidden` on an `<Icon>` (passed through `TooltipIconButton`'s
  boxed children) trips; the button's tooltip/label names each control.
- `type="button"` added to the locale toggle and the two unlock text links (were defaulting to submit).

## Tests

- `cargo test --workspace --lib --bins --tests` — **460 passed, 0 failed, 2 ignored** (the 2.8 Windows
  Hello human-smokes). No new host tests: 2.9.1 is layout + wiring + a new component with no pure logic;
  `entry_form`'s `to_payload`/`from_payload` (the field-model tests) stay green after the PEM control swap.

## Verification

- `cargo build -p vedge-app --target wasm32-unknown-unknown` — warning-free; `cargo build -p vedge-ui`
  clean.
- `cargo clippy -p vedge-app --target wasm32-unknown-unknown --no-deps -- -D warnings` — clean.
- `rustfmt --edition 2024 --check` clean on the touched leaf files.
- **Adversarial self-review** (per standing rule): every `t!`/`t_string!` read sits in a reactive
  closure / event-handler / `untrack`; the edit `Dialog`'s `ChildrenFn` captures only `Copy` values;
  `move_exclude` is a `StoredValue` read via `.get_value()` in the re-callable body; the `Input`
  `node_ref` reconciles with the private clear-focus ref; the `Textarea` `masked` class directive
  doesn't clobber the imperative auto-resize `style`; no plaintext/KEK crosses to WASM (the PEM reveal is
  display-only over `from_payload`-seeded model data).

## Manual smoke (human — `cargo tauri dev`, Windows)

Edit an Identity and an SSH key → a roomy centered dialog (not the 320px rail); the PEM is a multi-line
monospace textarea with a working reveal toggle. Shrink the window to the minimum → no horizontal
overflow, the three-pane holds. A very long URL/name in the list → truncates with a tooltip, no
horizontal scrollbar. Scrollbars are themed in light and dark. Onboarding scrolls at 600px height. The
window-chrome buttons are still keyboard/AT-labelled. Console stays warning-free (the i18n backstop).

## Component migration (completed follow-up — same branch)

The raw-`<button>`/`<input>` → design-system conversions were then **completed** (the user asked to
migrate them all). A full scan confirmed **no** native `<textarea>`/`<select>` remained anywhere.

**New design-system primitives / enablers:**
- **`Size::Xs`** (20px compact `IconButton`, 12px icon) — for dense row/tree action clusters where
  `Sm` (28px) would crowd; other components treat `Xs` as `Sm`.
- **`Variant::Link`** (transparent, no chrome, underline-on-hover) — for inline link-style text actions.
- **The `class` prop is now reactive (`TextProp`)** on `Button`, `IconButton`, and `SidebarItem`. The
  static `class` prop *shadowed* the reactive `class=("name", sig)` directive (Leptos "repeated field
  class"), which had blocked every control with a reactive selected/active class. This one change
  unblocked the nav-rows and the swatch grid; `attr:aria-pressed`/`attr:aria-current` carry toggle/nav
  ARIA.
- **`Input`** gained `input_ref` + `autofocus` (already) + **`aria_label`** (bound to the native
  `<input>`, for name-less rename/search fields).

**Converted:**
- Icon actions → `IconButton` (`Xs` in dense rows): `folder_tree` ×5, `smart_folders` ×3, `vault_list`
  pencil, `tag_assign` chip `×`.
- Text → `Button`: `folder_tree` breadcrumb → `Link`; `vault_unlock_panel` links → `Link`.
- Nav-rows → the design-system **`SidebarItem`**: `folder_tree` All / Unfiled (label + icon + count
  `badge` + `selected` → `aria-current`; the drag-drop drop-highlight rides `SidebarItem`'s reactive
  `class`, drag handlers spread onto its root button).
- Inputs → **`Input`**: `folder_tree` / `smart_folders` / `vault_list` new / rename / search
  (`autofocus` + `input_ref` for focus/`select()`; `on:keydown`/`on:focusout` spread bubble to the
  wrapper `<div>`, so Enter/Escape/arrow-nav + blur-commit still fire). `vault_list` search gained a
  leading magnifier icon via `Input`'s `leading_icon` slot.
- Toggle swatch → `IconButton` + reactive `class` + `attr:aria-pressed`: `folder_customize` icon grid.

**Kept raw — genuine design-system gaps (documented; `type="button"` for correctness):**
- The `/v` **collapsible sidebar** (nav rows + Lock + toggle): `SidebarItem` has no collapsed /
  icon-rail + tooltip mode, and converting only the native buttons would fragment the cohesive unit.
- The `window_panel` **locale toggle**: title-bar chrome using an *inherited computed* fg color (not a
  token) — a `<Button>` would impose the token color.
- The **round biometric hero button** (`vault_unlock_panel`): a 64px bespoke circle with a *reactive*
  `disabled` (`Button`'s `disabled` is a static bool).
- The **command-palette combobox `<input>`**: `role="combobox"` + `aria-*` must sit on the `<input>`;
  `Input` spreads attributes onto its wrapper `<div>`.
- Playground `pg_*` demos — dev-only.

**GUI smoke still required** to confirm the *look* of every converted control (sizing, hover,
focus-on-open, nav active state).

## Fixes from GUI testing

- **`Dialog` never closed (all dialogs).** The header ✕ / Escape / backdrop were a
  silent no-op on some dialog instances (reported on Manage-tags). Root cause found via a console
  trace: the open/close effect keyed its ENTER/EXIT branches off the effect's threaded `prev` return
  value, which went **stale** — the effect fired with `was=false` while the dialog was mounted, so
  `!now && was` was never true and EXIT never ran. Fixed by detecting transitions from the **real**
  `mounted`/`data_state` signals (read untracked, so `open` stays the only reactive dep) instead of
  `prev`. Robust regardless of what stales `prev`.
- **Entry detail → a right-side drawer.** The read-detail was a fixed 320px **3rd column** that crowded
  the table and spilled off-screen on narrow windows (next to the `/v` sidebar + a wide folder tree —
  the min-window guard alone couldn't fix it). It's now an **absolutely-positioned right drawer** that
  overlays the table (parent row `relative`); the folder tree + table get the full width, and the
  detail never competes for it. Opaque bg + shadow + a reduced-motion-aware `drawer-in` slide;
  dismiss via the header ✕.
- **Dialog ✕ never fired (all dialogs; playground too).** After the visibility fix above, the ✕ still
  didn't close — but backdrop, Escape, and footer buttons did. Root cause: the ✕'s handler was a
  **spread `on:click`** *forwarded onto the `IconButton` component*, and the Dialog renders inside a
  `<Portal>`. Leptos's spread-onto-component forwarding does not reliably attach a listener across the
  portal boundary, so that one click silently never ran `on_close` (every *direct* handler — scrim,
  Escape, the old plain-`<button>` ✕ — works in the same Portal). Fix: `IconButton` gains a first-class
  **`on_click: Option<Callback<()>>`** prop that binds `on:click` **directly** on its `<button>`
  (the Leptos-recommended "handlers as props"); `DialogHeader` uses `on_click=ctx.on_close`. Fixes the
  close button for **every** dialog at once. Event delegation is off by default in Leptos 0.8, so the
  direct listener coexists with any spread `on:click` (native listeners stack) — existing spread call
  sites are untouched. A comment in `header.rs` warns against "simplifying" it back to spread `on:click`.

## Deviations (recorded in the tracking note)

1. **`DialogSize::Lg` already existed** — the spec's "add `Lg`" premise was stale (no enum/CSS change
   needed).
2. **PEM reveal masks via `-webkit-text-security`** (both target webviews honour it) rather than a
   two-textarea swap; the reveal is display-only over the already-revealed model value.
3. **Create stays an inline width-capped panel; only edit moved to the dialog** (resolving the spec's
   internal #1-vs-#6 contradiction in favour of #6 + the mockup, which only depicts edit-in-dialog).
4. **A handful of controls stay raw by design** (the "genuine design-system gaps" list above) — every
   other native `<button>`/`<input>` in the app is now on the design system.
