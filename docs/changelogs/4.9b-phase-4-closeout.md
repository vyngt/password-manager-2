# Slice 4.9b — Phase-4 Close-out (part 2 of 2)

The **verify** half of the final Phase-4 slice. 4.9a shipped the UX polish (PR #48); 4.9b runs the
close-out **over that frozen tree** — a close-out's job is to verify, so it branches off a `develop`
that already contains 4.9a and touches **no feature code** (two one-line doc-comment edits + a docs
reconcile + the gates + the roadmap flip). It closes Phase 4.

The pickup re-scan found two close-out items were **already done ahead of time** during the Phase-5
"peek": the WASM-secret sentinel disposition (C6) and the Phase-5 seed (C8). Those collapsed to
*verify + bless* rather than *create*. 🔴 corrections the scan forced are called out below.

## C1 — Pin ONE canonical test-count command, and restate the ledger

The Phase-4 ledger drifted **579 → 600 → 532 → 522** and does not reconcile — the numbers counted
different scopes. The Iteration Standards *mandate* a test count for velocity calibration, so
inconsistent numbers are worse than none (they look authoritative).

- 🔴 **Correction:** the shipped `mise test` task is `cargo test --workspace --exclude vedge-e2e
  --lib --bins --tests` (doctests **excluded**) — not the spec's proposed `cargo test --workspace
  --exclude vedge-e2e`. Pinned the **exact `mise test` string** in the Iteration Standards (§4
  *Codebase gates* + the Retro bullet + §7), so the number always matches the gate.
- **Two independent numbers, never blended:** **host = 638** (0 failed, 2 ignored) from that command;
  **e2e scenarios = 11** (`mise e2e`). Baseline recorded in the Standards; the broken 579/600/532/522
  series is retired (regenerate from the command, don't chase an integer).

## C2 — Close-out-checklist violations (the 4 shipped-with-gaps slices)

- **4.5a** — `Shipped:` line was still `(PR # — pending push)` → **PR #44**; pre-flight boxes ticked;
  **Mid-iteration decisions** filled (the `ScheduledJob`-drop rationale, the fail-closed poll, the
  `SystemTime`/absolute-TTL decisions, the poll+toast backstop).
- **4.4** — pre-flight boxes ticked; **Mid-iteration decisions** filled (Scored-only breach fan-out,
  the `breach_checked` false-all-clear guard, the "zero new deps" hold, the GitGuardian squash).
- **4.5b** — the **Demo block** and the "Demo runs" Status parenthetical still said the on-device smoke
  was **PENDING** while Status said passed → reconciled to **DONE (2026-07-12, non-inverted)**;
  "ready for merge" → "merged".
- **4.6** — fm `status: planned` → **shipped**; pre-flight ticked; **Mid-iteration decisions** filled
  (both sub-slices: additive `vault_uuid` + per-task jobs + `pending_locks`; the D2-full→D2-Alt pivot +
  the `try_*` sweep + the Select `Memo`).
- **Demo-output blocks** (4.4/4.5a/4.6) were filled with output **captured from this close-out's gate
  run** (the honest fill for a retroactive close-out — the slices shipped without capturing theirs).

## C3 — Docs reconciled to as-built

- **Code (2 doc-comments):** `domain/vault/health.rs` — the `AgeConfidence::Estimated` comment dropped
  the stale `entry_history` tier (`scan_health::assess_age` uses only `secret_changed_at` else
  `created_at`; it never walks history). `ARCHITECTURE.md` — the WASM-secret "backlogged follow-up"
  line now points at its **Phase-5** disposition (generalize the 4.2 `TotpUpdate` sentinel).
- **`20.2 Vault Storage Schema`:** added **`vault_uuid`** to the `vault_config` block + a prose
  paragraph (nullable `TEXT` ULID, additive, backfilled on both unlock paths, the LAN-Sync
  prerequisite, single-row-preserving). De-Elixired the stale sidecar claims: the **Login/HIBP** flow
  is Rust in `vedge-tauri` (slice 4.4), and the **ApiKey/expiry** monitoring + the 2026-07-09 callout
  now say *in-app scheduled job — there is no sidecar* (retired).
- **`02 Architecture/13 - Vault Identity`:** added an **adopted (4.6a, PR #46)** banner.
- **`05 Research/Core Hardening/02 - Sidecar Trust Boundary`:** fm `draft` → `active` (its 4.4 adopted
  banners were already present).
- **Verified-only (already correct):** `common_meta.rs:56` (falls back to `created_at`) and the
  `TOTPDisplay` "secret-agnostic (4.2)" banner — no edit.

## C4 — Promoted 6 stranded lessons into the Iteration Standards

The 5 the spec named, **+ a 6th the Phase-5 backlog explicitly asked to promote at 4.9b** (recorded as
a +1 DRIFT vs the spec's "5", per Standards §8 *trust the vault*):

1. **Irreducible on-device gates — ship the mechanism, gate + log the unverifiable bit** (4.5b) → §6.
2. **The pickup scan must re-verify CONSTRAINTS and BLAST RADIUS, not just signatures** (4.5a/4.6a) → §2.
3. **`cargo check` clean ≠ green — verify the wasm *target* AND the *link*** (4.2 / 4.6b / 4.9a) → §4 gate.
4. **Spawn each scheduled job on its own task** so a slow/panicking one can't stall a security-critical one (4.6a) → §3.
5. **GitGuardian scans every commit in a PR — no password-shaped literals in test vectors** (4.4) → §7.
6. **A dead-code sweep can't tell "unused" from "not yet wired"** (2.10.1 killed retention *and* recovery) → §7.

Plus the footnote (`mise fmt` unreliable → `cargo fmt --all`; clippy **after** leptosfmt) → §3.

## C5 — Human smokes (owner-run — handed off)

I cannot drive these. Per the promoted lesson #1, 4.9b ships on **gates-green** with the on-device
smokes recorded as owner-run:

| # | Smoke | Status |
|---|---|---|
| 1 | 🔴 **3.2 clipboard — Win+V** (declared 4.2 gate; TOTP copy shares the hardened path) | **OVERDUE — please run** |
| 2 | Real Windows Hello (enroll→lock→prompt→unlock) | carried |
| 3 | 3.4 generator modes GUI (5 modes → honest bands) | carried |
| 4 | 4.6a Settings → Maintenance "Run now" → report shows counts | new |
| 5 | 4.5b screen-lock | ✅ done 2026-07-12 |

## C6 — WASM-secret sentinel dispositioned (verified — was pre-done)

`get_entry` still ships `password` / `recovery_codes` / card / SSH / API secrets to WASM as plain
`String` (only `totp_secret` is sealed via 4.2's `TotpUpdate::{Unchanged,Set,Clear}`). **Disposition:
Phase 5 slice 5.7** — generalize the 4.2 sentinel. Confirmed recorded in both Phase-5 notes;
`ARCHITECTURE.md` synced. *(Also confirmed the reported `copy_field.rs` "binary file" flag was a
`LC_ALL=C` grep artifact — the file is clean UTF-8. Nothing to fix.)*

## C7 — Gates (see Verification)

## C8 — Roadmap flipped + Phase 5 blessed

- **Roadmap:** Phase 4 `🚧 Active` → **✅ Done** (slices 4.1, 4.2, 4.3a/b, 4.4, 4.5a/b, 4.6a/b, 4.9a/b;
  PRs #39–#48 + this close-out; **"there is no 4.7 or 4.8"**). Phase 5 `⬜ Scaffolded` → **🚧 Active**.
- **Phase 5 seed (verified + blessed):** the `Phase 5 - Data Safety & Recovery/` folder was already
  seeded during the peek (Iteration Plan with slices 5.1–5.9, why-not-CSV, the unmeasured O(n) unlock,
  the recovery dead-end that leads the phase; a finalized Backlog; `Slices/` + `Specs/`). Bumped both
  seed notes' fm `planned` → `active` and updated the 3 Core-Hardening notes' **Adoption** sections to
  their Phase-5 dispositions (`01` Encrypted VaultIndex → conditional/5.6; `04` Memory-Hygiene item 4
  → format-freeze defused; `06` Rollback Detection → folded into 5.2, keyable on `vault_uuid`).

## Verification

All gates green over the frozen tree (2026-07-13):

- `cargo fmt --all` + leptosfmt · **`mise ci`** (fmt-check + clippy `-D warnings` + host tests + wasm32
  check) — **EXIT 0**.
- **Host tests: 638 passed, 0 failed, 2 ignored** (`cargo test --workspace --exclude vedge-e2e --lib
  --bins --tests`).
- **Full-debuginfo wasm LINK** (`cargo build -p vedge-app --target wasm32-unknown-unknown`) — **EXIT 0,
  no `rust-lld 0xC0000005`** (4.9a's `.cargo/config.toml` `debuginfo=0` fix holds).
- **`mise audit`** (cargo-deny — no new crate) — **EXIT 0**.
- **`mise e2e` — 11/11 passed, console-clean** (the reactive-context warning guard is compiled into the
  debug wasm and stayed silent).

**Self-review:** the repo diff is docs-only — two doc-comments (`health.rs` age-confidence,
`ARCHITECTURE.md` WASM-secret disposition) + this changelog — so a manual adversarial pass (both
statements verified against `scan_health::assess_age` and the 4.2 sentinel) was proportionate rather
than the multi-agent swarm. No component/reactive code changed.

**Human smokes (owner-run — handed off, not blocking):** 🔴 **Win+V clipboard** (overdue — TOTP copy
shares the hardened path) · Windows Hello · 3.4 generator modes GUI · 4.6a Settings → Maintenance "Run
now". Per the promoted "irreducible on-device gate" lesson, the phase closes on gates-green with these
logged for the owner.
